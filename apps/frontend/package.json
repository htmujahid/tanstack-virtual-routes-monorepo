{
  "name": "@repo/frontend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build && tsc --noEmit",
    "serve": "vite preview",
    "start": "vite"
  }
}
